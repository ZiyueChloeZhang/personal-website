---
interface Props {
    title: string;
}

const { title } = Astro.props;
---
<section>
    <h2>{title}</h2>
    <slot/>
</section>

<style>
    section {
        display: grid;
        gap: 1rem;
    }
</style>